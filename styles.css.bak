/* ==========================================================================
   NEBULA MENTOR - GLASSMORPHISM DESIGN SYSTEM
   ========================================================================== */

@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap');

:root {
  /* Google Light Mode Palette */
  --bg-dark: #f8f9fa;
  --bg-gradient: none;
  --card-bg: #ffffff;
  --card-hover-bg: #ffffff;
  --border-light: #dadce0;
  --border-glow: rgba(66, 133, 244, 0.4);
  --text-primary: #202124;
  --text-secondary: #5f6368;
  --text-muted: #80868b;
  --overlay-rgb: 0, 0, 0;
}

body.dark-mode {
  --bg-dark: #202124;
  --card-bg: #303134;
  --card-hover-bg: #3c4043;
  --border-light: #5f6368;
  --text-primary: #e8eaed;
  --text-secondary: #9aa0a6;
  --text-muted: #80868b;
  --overlay-rgb: 255, 255, 255;
}
  
  /* Section Theme Accents */
  --color-primary-school: #fbbc05;
  --color-high-school: #34a853;
  --color-competitive: #ea4335;
  
  --primary-gradient: linear-gradient(135deg, #4285f4, #1a73e8);
  --accent-glow: none;
  
  /* Font Families */
  --font-display: 'Roboto', sans-serif;
  --font-body: 'Roboto', sans-serif;
  
  /* Layout Constants */
  --sidebar-width: 260px;
  --header-height: 70px;
  
  /* Animations */
  --transition-smooth: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Base Styles */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  scrollbar-width: thin;
  scrollbar-color: rgba(var(--overlay-rgb), 0.1) transparent;
}

body {
  font-family: var(--font-body);
  background: var(--bg-dark);
  background-image: var(--bg-gradient);
  background-attachment: fixed;
  color: var(--text-primary);
  min-height: 100vh;
  overflow-x: hidden;
  display: flex;
}

a {
  color: inherit;
  text-decoration: none;
}

button, input, select, textarea {
  font-family: inherit;
  outline: none;
  appearance: none;
  cursor: pointer;
  color: var(--text-primary);
  background: none;
}

button {
  cursor: pointer;
  border: none;
  background: none;
}

/* Scrollbars */
::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}
::-webkit-scrollbar-track {
  background: transparent;
}
::-webkit-scrollbar-thumb {
  background: rgba(var(--overlay-rgb), 0.1);
  border-radius: 4px;
}
::-webkit-scrollbar-thumb:hover {
  background: rgba(var(--overlay-rgb), 0.2);
}

/* App Container Layout */
.app-container {
  display: flex;
  width: 100vw;
  min-height: 100vh;
}

/* Sidebar Navigation */
.sidebar {
  width: var(--sidebar-width);
  background: var(--card-bg);
  border-right: 1px solid var(--border-light);
  display: flex;
  flex-direction: column;
  padding: 24px 16px;
  position: fixed;
  height: 100vh;
  z-index: 100;
  transition: var(--transition-smooth);
}

.logo-container {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 12px 24px 12px;
  border-bottom: 1px solid var(--border-light);
  margin-bottom: 24px;
}

.logo-icon {
  width: 36px;
  height: 36px;
  background: var(--primary-gradient);
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 20px;
  font-weight: 800;
  font-family: var(--font-display);
  box-shadow: none;
}

.theme-toggle {
  background: rgba(var(--overlay-rgb), 0.05);
  border: 1px solid var(--border-light);
  color: var(--text-primary);
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
  margin-right: 16px;
  transition: var(--transition-smooth);
}

.theme-toggle:hover {
  background: rgba(var(--overlay-rgb), 0.1);
}

.logo-text {
  font-family: var(--font-display);
  font-size: 1.2rem;
  font-weight: 700;
  letter-spacing: 0.5px;
  color: var(--text-primary);
}

.nav-menu {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 8px;
  flex-grow: 1;
}

.nav-item {
  width: 100%;
}

.nav-link {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 12px 16px;
  border-radius: 0 24px 24px 0;
  margin-right: 16px;
  color: var(--text-secondary);
  font-size: 0.95rem;
  font-weight: 500;
  transition: var(--transition-smooth);
  position: relative;
  overflow: hidden;
}

.nav-link i {
  font-size: 1.1rem;
  width: 20px;
  text-align: center;
}

.nav-link:hover {
  color: var(--text-primary);
  background: rgba(var(--overlay-rgb), 0.04);
}

.nav-item.active .nav-link {
  color: #fff;
  background: rgba(138, 43, 226, 0.15);
  border: 1px solid rgba(138, 43, 226, 0.3);
}

.nav-item.active .nav-link::before {
  content: '';
  position: absolute;
  left: 0;
  top: 15%;
  height: 70%;
  width: 4px;
  background: var(--primary-gradient);
  border-radius: 0 4px 4px 0;
}

.sidebar-footer {
  margin-top: auto;
  border-top: 1px solid var(--border-light);
  padding-top: 16px;
}

/* Main Dashboard Area */
.main-content {
  margin-left: var(--sidebar-width);
  flex-grow: 1;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  transition: var(--transition-smooth);
}

/* Header */
.top-header {
  height: var(--header-height);
  background: rgba(7, 6, 15, 0.4);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--border-light);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 40px;
  position: sticky;
  top: 0;
  z-index: 90;
}

.welcome-widget h1 {
  font-family: var(--font-display);
  font-size: 1.3rem;
  font-weight: 600;
}

.header-right {
  display: flex;
  align-items: center;
  gap: 24px;
}

.clock-widget {
  font-family: var(--font-display);
  font-size: 1.1rem;
  font-weight: 500;
  background: rgba(var(--overlay-rgb), 0.03);
  padding: 6px 14px;
  border-radius: 30px;
  border: 1px solid var(--border-light);
  display: flex;
  align-items: center;
  gap: 8px;
}

.clock-widget i {
  color: var(--color-high-school);
  animation: pulse 2s infinite;
}

.api-status-btn {
  background: rgba(var(--overlay-rgb), 0.03);
  border: 1px solid var(--border-light);
  padding: 8px 14px;
  border-radius: 12px;
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--text-secondary);
  display: flex;
  align-items: center;
  gap: 8px;
  transition: var(--transition-smooth);
}

.api-status-btn:hover {
  background: rgba(var(--overlay-rgb), 0.08);
  color: var(--text-primary);
}

.api-status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #ff4757; /* Off by default */
}

.api-status-dot.connected {
  background: #2ed573;
  box-shadow: 0 0 8px #2ed573;
}

/* Panels Router Containers */
.content-panel {
  display: none;
  padding: 40px;
  flex-grow: 1;
  animation: fadeIn 0.4s ease-out;
}

.content-panel.active {
  display: block;
}

/* Glass Card Styling */
.glass-card {
  background: var(--card-bg);
  border: 1px solid var(--border-light);
  border-radius: 8px;
  padding: 24px;
  transition: var(--transition-smooth);
  box-shadow: 0 1px 2px 0 rgba(60,64,67,0.3), 0 1px 3px 1px rgba(60,64,67,0.15);
}

.glass-card:hover {
  background: var(--card-hover-bg);
  border-color: rgba(var(--overlay-rgb), 0.12);
  box-shadow: 0 1px 3px 0 rgba(60,64,67,0.3), 0 4px 8px 3px rgba(60,64,67,0.15);
}

/* Grid Layouts */
.grid-2col {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24px;
}

.grid-3col {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

/* ==========================================================================
   PANEL: DASHBOARD
   ========================================================================== */
.db-hero {
  grid-column: span 2;
  background: linear-gradient(135deg, rgba(138, 43, 226, 0.15) 0%, rgba(74, 0, 224, 0.05) 100%);
  border: 1px solid var(--border-glow);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.db-hero-info h2 {
  font-family: var(--font-display);
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 8px;
}

.db-hero-info p {
  color: var(--text-secondary);
  font-size: 1rem;
  max-width: 500px;
  line-height: 1.5;
}

.level-cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
  margin-top: 24px;
}

.level-card {
  text-align: center;
  cursor: pointer;
  position: relative;
  overflow: hidden;
}

.level-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 4px;
}

.level-card.primary::before { background: var(--color-primary-school); }
.level-card.high::before { background: var(--color-high-school); }
.level-card.comp::before { background: var(--color-competitive); }

.level-icon {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 16px auto;
  font-size: 1.5rem;
}

.level-card.primary .level-icon { background: rgba(255, 159, 67, 0.15); color: var(--color-primary-school); }
.level-card.high .level-icon { background: rgba(0, 210, 211, 0.15); color: var(--color-high-school); }
.level-card.comp .level-icon { background: rgba(165, 94, 234, 0.15); color: var(--color-competitive); }

.level-card h3 {
  font-family: var(--font-display);
  margin-bottom: 8px;
  font-size: 1.2rem;
}

.level-card p {
  color: var(--text-secondary);
  font-size: 0.85rem;
  line-height: 1.4;
}

.db-quick-tools {
  margin-top: 24px;
}

.db-title {
  font-family: var(--font-display);
  font-size: 1.4rem;
  font-weight: 600;
  margin-bottom: 18px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.stat-item {
  background: rgba(var(--overlay-rgb), 0.02);
  border: 1px solid var(--border-light);
  border-radius: 12px;
  padding: 16px;
  text-align: center;
}

.stat-value {
  font-family: var(--font-display);
  font-size: 1.8rem;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 4px;
}

.stat-label {
  font-size: 0.8rem;
  color: var(--text-secondary);
}

/* ==========================================================================
   PANEL: YOUR PROFESSOR
   ========================================================================== */
.chat-container {
  display: flex;
  height: calc(100vh - var(--header-height) - 80px);
}

.chat-main {
  flex-grow: 1;
  display: flex;
  flex-direction: column;
  border-radius: 18px;
  overflow: hidden;
}

.chat-header-bar {
  padding: 16px 24px;
  background: rgba(var(--overlay-rgb), 0.02);
  border-bottom: 1px solid var(--border-light);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.chat-messages {
  flex-grow: 1;
  padding: 24px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.chat-bubble {
  max-width: 75%;
  padding: 14px 18px;
  border-radius: 16px;
  font-size: 0.95rem;
  line-height: 1.5;
  animation: messageSlide 0.3s cubic-bezier(0.18, 0.89, 0.32, 1.28);
  word-wrap: break-word;
}

.chat-bubble.user {
  background: var(--primary-gradient);
  align-self: flex-end;
  border-bottom-right-radius: 4px;
  color: #fff;
  box-shadow: var(--accent-glow);
}

.chat-bubble.ai {
  background: rgba(var(--overlay-rgb), 0.05);
  border: 1px solid var(--border-light);
  align-self: flex-start;
  border-bottom-left-radius: 4px;
}

.chat-bubble pre {
  background: rgba(var(--overlay-rgb), 0.3);
  padding: 10px;
  border-radius: 8px;
  overflow-x: auto;
  margin-top: 10px;
  border: 1px solid var(--border-light);
}

.chat-bubble code {
  font-family: monospace;
  font-size: 0.85rem;
}

.chat-bubble p {
  margin-bottom: 8px;
}
.chat-bubble p:last-child {
  margin-bottom: 0;
}

.chat-bubble strong {
  color: #fff;
}

.chat-bubble ul, .chat-bubble ol {
  margin-left: 20px;
  margin-bottom: 8px;
}

.chat-input-bar {
  padding: 16px 24px;
  background: rgba(10, 8, 24, 0.5);
  border-top: 1px solid var(--border-light);
  display: flex;
  gap: 12px;
}

.chat-input {
  flex-grow: 1;
  background: rgba(var(--overlay-rgb), 0.03);
  border: 1px solid var(--border-light);
  border-radius: 12px;
  padding: 12px 18px;
  color: #fff;
  font-size: 0.95rem;
  transition: var(--transition-smooth);
}

.chat-input:focus {
  border-color: rgba(66, 133, 244, 0.5);
  box-shadow: 0 0 15px rgba(66, 133, 244, 0.15);
  background: rgba(var(--overlay-rgb), 0.05);
}

.chat-send-btn {
  width: 48px;
  height: 48px;
  background: var(--primary-gradient);
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 1.1rem;
  transition: var(--transition-smooth);
}

.chat-send-btn:hover {
  transform: translateY(-2px);
  box-shadow: var(--accent-glow);
}

/* ==========================================================================
   PANEL: VIDEO LIBRARY
   ========================================================================== */
.search-filter-row {
  display: flex;
  gap: 16px;
  margin-bottom: 24px;
}

.video-search-wrapper {
  position: relative;
  flex-grow: 1;
}

.video-search-wrapper i {
  position: absolute;
  left: 18px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--text-secondary);
}

.video-search-input {
  width: 100%;
  background: var(--card-bg);
  border: 1px solid var(--border-light);
  border-radius: 14px;
  padding: 14px 14px 14px 48px;
  color: #fff;
  font-size: 0.95rem;
  transition: var(--transition-smooth);
}

.video-search-input:focus {
  border-color: rgba(66, 133, 244, 0.5);
  box-shadow: var(--neon-glow);
}

.filter-select {
  background: var(--card-bg);
  color: #fff;
  border: 1px solid var(--border-light);
  padding: 0 20px;
  border-radius: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: var(--transition-smooth);
}

.filter-select:hover {
  border-color: rgba(var(--overlay-rgb), 0.2);
}

.video-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 24px;
}

.video-card {
  cursor: pointer;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.video-thumbnail {
  position: relative;
  width: 100%;
  aspect-ratio: 16/9;
  border-radius: 12px;
  overflow: hidden;
  background: #000;
  margin-bottom: 14px;
}

.video-thumbnail img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.5s ease;
}

.video-card:hover .video-thumbnail img {
  transform: scale(1.05);
}

.play-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(var(--overlay-rgb), 0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  transition: var(--transition-smooth);
  outline: none;
  font-size: 0.95rem;
  color: var(--text-primary);
  text-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
}

.video-card:hover .play-overlay {
  opacity: 1;
}

.play-overlay i {
  font-size: 2.5rem;
  color: #fff;
  text-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
}

.channel-tag {
  position: absolute;
  bottom: 8px;
  left: 8px;
  background: rgba(var(--overlay-rgb), 0.7);
  padding: 4px 8px;
  border-radius: 6px;
  margin-top: 10px;
  font-size: 0.7rem;
  font-weight: 600;
  color: var(--text-primary);
}

.video-info h4 {
  font-size: 0.95rem;
  font-weight: 600;
  line-height: 1.4;
  margin-bottom: 6px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.video-meta {
  display: flex;
  justify-content: space-between;
  font-size: 0.75rem;
  color: var(--text-secondary);
}

/* ==========================================================================
   PANEL: FOCUS HUB
   ========================================================================== */
.focus-container {
  display: grid;
  grid-template-columns: 1.2fr 0.8fr;
  gap: 32px;
}

.focus-main-panel {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40px;
}

.timer-display-wrapper {
  position: relative;
  width: 280px;
  height: 280px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 30px;
}

.timer-svg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  transform: rotate(-90deg);
}

.timer-track-circle {
  fill: none;
  stroke: rgba(var(--overlay-rgb), 0.03);
  stroke-width: 8;
}

.timer-progress-circle {
  fill: none;
  stroke: var(--primary-gradient);
  stroke-width: 8;
  stroke-linecap: round;
  stroke-dasharray: 816; /* 2 * PI * 130 */
  stroke-dashoffset: 0;
  transition: stroke-dashoffset 0.1s linear;
}

.timer-text-container {
  z-index: 10;
  text-align: center;
}

.timer-clock {
  font-family: var(--font-display);
  font-size: 3.5rem;
  font-weight: 700;
  letter-spacing: -1px;
}

.timer-phase {
  font-size: 0.85rem;
  color: var(--text-secondary);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 2px;
  margin-top: 4px;
}

.timer-controls {
  display: flex;
  gap: 16px;
  margin-bottom: 24px;
}

.btn-timer {
  width: 54px;
  height: 54px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
  border: 1px solid var(--border-light);
  background: rgba(var(--overlay-rgb), 0.03);
  transition: var(--transition-smooth);
}

.btn-timer:hover {
  background: rgba(var(--overlay-rgb), 0.08);
  border-color: rgba(var(--overlay-rgb), 0.2);
}

.btn-timer.primary {
  background: var(--primary-gradient);
  color: #fff;
  border: none;
  box-shadow: var(--accent-glow);
}

.btn-timer.primary:hover {
  transform: scale(1.05);
}

.timer-presets {
  display: flex;
  gap: 12px;
}

.btn-preset {
  background: rgba(var(--overlay-rgb), 0.03);
  border: 1px solid var(--border-light);
  padding: 8px 16px;
  border-radius: 20px;
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--text-secondary);
  transition: var(--transition-smooth);
}

.btn-preset.active {
  background: rgba(66, 133, 244, 0.1);
  border-color: var(--primary-gradient);
  color: #1a73e8;
}

/* Ambient sound cards */
.ambient-panel h3 {
  font-family: var(--font-display);
  margin-bottom: 20px;
}

.ambient-list {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.ambient-card {
  background: rgba(var(--overlay-rgb), 0.02);
  border: 1px solid var(--border-light);
  border-radius: 14px;
  padding: 14px 18px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.ambient-info {
  display: flex;
  align-items: center;
  gap: 12px;
}

.ambient-icon {
  width: 38px;
  height: 38px;
  border-radius: 10px;
  background: rgba(var(--overlay-rgb), 0.04);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
}

.ambient-card.active .ambient-icon {
  background: rgba(138, 43, 226, 0.15);
  color: var(--color-competitive);
}

.ambient-name {
  font-size: 0.9rem;
  font-weight: 500;
}

.sound-control-wrap {
  display: flex;
  align-items: center;
  gap: 12px;
  flex-grow: 1;
  max-width: 140px;
}

.btn-ambient-play {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.9rem;
  background: linear-gradient(135deg, rgba(255,255,255,0.1), rgba(255,255,255,0.02));
  border: 1px solid rgba(255,255,255,0.1);
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  color: var(--text-primary);
  cursor: pointer;
}

.btn-ambient-play:hover {
  background: var(--primary-gradient);
  transform: translateY(-2px);
  box-shadow: 0 6px 15px rgba(66, 133, 244, 0.4);
  color: #fff;
  border-color: transparent;
}

.ambient-slider {
  flex-grow: 1;
  -webkit-appearance: none;
  background: rgba(var(--overlay-rgb), 0.1);
  height: 6px;
  border-radius: 3px;
  outline: none;
  transition: all 0.3s ease;
  box-shadow: inset 0 1px 3px rgba(0,0,0,0.3);
}

.ambient-slider:hover {
  background: rgba(var(--overlay-rgb), 0.2);
}

.ambient-slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--primary-color, #4285f4);
  cursor: pointer;
  border: 2px solid #fff;
  box-shadow: 0 0 10px rgba(66, 133, 244, 0.6);
  transition: transform 0.2s;
}

.ambient-slider::-webkit-slider-thumb:hover {
  transform: scale(1.2);
}

/* Dynamic Focus overlay background tint */
.focus-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: rgba(5, 4, 10, 0.98);
  z-index: 1000;
  display: none;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  animation: fadeIn 0.5s ease;
}

.focus-overlay.active {
  display: flex;
}

.focus-overlay-timer {
  font-family: var(--font-display);
  font-size: 8rem;
  font-weight: 800;
  margin-bottom: 20px;
}

/* ==========================================================================
   PANEL: STUDY PLANNER & CALENDAR
   ========================================================================== */
.planner-layout {
  display: grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 32px;
}

.calendar-card {
  padding: 24px;
}

.calendar-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}

.calendar-month-year {
  font-family: var(--font-display);
  font-size: 1.15rem;
  font-weight: 700;
}

.calendar-nav-btn {
  width: 32px;
  height: 32px;
  border-radius: 8px;
  border: 1px solid var(--border-light);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: var(--transition-smooth);
}

.calendar-nav-btn:hover {
  background: rgba(var(--overlay-rgb), 0.05);
}

.calendar-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 8px;
  text-align: center;
}

.calendar-weekday {
  font-size: 0.75rem;
  font-weight: 600;
  color: var(--text-muted);
  text-transform: uppercase;
  padding: 8px 0;
}

.calendar-day {
  aspect-ratio: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 10px;
  font-size: 0.85rem;
  font-weight: 500;
  cursor: pointer;
  position: relative;
  transition: var(--transition-smooth);
  border: 1px solid transparent;
}

.calendar-day:hover {
  background: rgba(var(--overlay-rgb), 0.04);
}

.calendar-day.inactive {
  color: var(--text-muted);
  opacity: 0.3;
}

.calendar-day.today {
  border-color: rgba(66, 133, 244, 0.4);
  background: rgba(66, 133, 244, 0.08);
}

.calendar-day.selected {
  background: var(--primary-gradient);
  color: #fff;
  box-shadow: var(--accent-glow);
}

.day-dot-container {
  display: flex;
  gap: 3px;
  position: absolute;
  bottom: 4px;
}

.calendar-dot {
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: var(--color-high-school);
}

/* Scheduler / To-do check */
.todo-card {
  display: flex;
  flex-direction: column;
  max-height: 520px;
}

.todo-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px 32px;
  color: var(--text-primary);
}

.todo-list {
  flex-grow: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.todo-item {
  background: rgba(var(--overlay-rgb), 0.02);
  border: 1px solid var(--border-light);
  border-radius: 12px;
  padding: 14px 16px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: var(--transition-smooth);
}

.todo-item:hover {
  border-color: rgba(var(--overlay-rgb), 0.15);
}

.todo-item-left {
  display: flex;
  align-items: center;
  gap: 12px;
}

.todo-checkbox {
  width: 20px;
  height: 20px;
  border-radius: 6px;
  border: 2px solid var(--border-light);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: var(--transition-smooth);
  outline: none;
  font-size: 0.9rem;
  color: var(--text-primary);
  opacity: 0;
  transition: var(--transition-smooth);
}

.todo-checkbox i {
  font-size: 0.7rem;
  color: #fff;
  opacity: 0;
  transition: var(--transition-smooth);
}

.todo-item.completed .todo-checkbox {
  background: #2ed573;
  border-color: #2ed573;
}

.todo-item.completed .todo-checkbox i {
  opacity: 1;
}

.todo-item.completed .todo-text {
  text-decoration: line-through;
  color: var(--text-secondary);
}

.todo-text {
  font-size: 0.9rem;
  font-weight: 500;
}

.todo-badge {
  font-size: 0.7rem;
  padding: 3px 8px;
  border-radius: 10px;
  font-weight: 600;
  text-transform: uppercase;
}

.todo-badge.primary { background: rgba(255, 159, 67, 0.15); color: var(--color-primary-school); }
.todo-badge.high { background: rgba(0, 210, 211, 0.15); color: var(--color-high-school); }
.todo-badge.comp { background: rgba(165, 94, 234, 0.15); color: var(--color-competitive); }

.btn-todo-delete {
  color: var(--text-muted);
  transition: var(--transition-smooth);
}

.btn-todo-delete:hover {
  color: #ff4757;
}

/* ==========================================================================
   PANEL: RESOURCES VAULT
   ========================================================================== */
.res-categories {
  display: flex;
  gap: 12px;
  margin-bottom: 24px;
}

.res-btn {
  background: rgba(var(--overlay-rgb), 0.03);
  border: 1px solid var(--border-light);
  padding: 10px 20px;
  border-radius: 20px;
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--text-secondary);
  transition: var(--transition-smooth);
}

.res-btn:hover {
  background: rgba(var(--overlay-rgb), 0.08);
}

.res-btn.active {
  background: var(--primary-gradient);
  border-color: transparent;
  color: #fff;
  box-shadow: var(--accent-glow);
}

.resources-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 24px;
}

.resource-card {
  position: relative;
  display: flex;
  flex-direction: column;
}

.resource-badge {
  position: absolute;
  top: 18px;
  right: 18px;
  background: rgba(var(--overlay-rgb), 0.08);
  padding: 4px 10px;
  border-radius: 8px;
  font-size: 0.7rem;
  font-weight: 600;
}

.resource-card h3 {
  font-family: var(--font-display);
  font-size: 1.15rem;
  margin-bottom: 12px;
}

.resource-desc {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.5;
  margin-bottom: 18px;
}

.resource-link {
  margin-top: auto;
  align-self: flex-start;
  display: flex;
  align-items: center;
  gap: 8px;
  color: var(--color-high-school);
  font-size: 0.85rem;
  font-weight: 600;
  transition: var(--transition-smooth);
}

.resource-card:hover .resource-link {
  gap: 12px;
}

/* ==========================================================================
   MODAL CONTAINER & UI WIDGETS
   ========================================================================== */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: rgba(5, 4, 10, 0.8);
  backdrop-filter: blur(10px);
  z-index: 1000;
  display: none;
  align-items: center;
  justify-content: center;
}

.modal-overlay.active {
  display: flex;
}

.modal-content {
  background: #0f0c24;
  border: 1px solid var(--border-light);
  border-radius: 20px;
  width: 90%;
  max-width: 540px;
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.6);
  overflow: hidden;
  animation: modalScale 0.3s cubic-bezier(0.18, 0.89, 0.32, 1.28);
}

.modal-content.large {
  max-width: 800px;
}

.modal-header {
  padding: 20px 24px;
  border-bottom: 1px solid var(--border-light);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.modal-header h3 {
  font-family: var(--font-display);
  font-size: 1.2rem;
  font-weight: 600;
}

.btn-close-modal {
  font-size: 1.2rem;
  color: var(--text-secondary);
  transition: var(--transition-smooth);
}

.btn-close-modal:hover {
  color: #fff;
}

.modal-body {
  padding: 24px;
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--text-secondary);
  margin-bottom: 8px;
}

.form-input {
  width: 100%;
  background: rgba(var(--overlay-rgb), 0.03);
  border: 1px solid var(--border-light);
  border-radius: 10px;
  padding: 10px 14px;
  color: #fff;
  font-size: 0.9rem;
  transition: var(--transition-smooth);
}

.form-input:focus {
  border-color: rgba(66, 133, 244, 0.5);
  box-shadow: var(--neon-glow);
}

.form-select {
  width: 100%;
  background: rgba(var(--overlay-rgb), 0.03);
  border: 1px solid var(--border-light);
  border-radius: 10px;
  padding: 10px 14px;
  color: #fff;
  font-size: 0.9rem;
  cursor: pointer;
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  margin-top: 28px;
}

.btn-form {
  padding: 10px 20px;
  border-radius: 10px;
  font-size: 0.85rem;
  font-weight: 600;
  transition: var(--transition-smooth);
}

.btn-form.cancel {
  background: rgba(var(--overlay-rgb), 0.05);
  border: 1px solid var(--border-light);
  color: var(--text-secondary);
}

.btn-form.cancel:hover {
  background: rgba(var(--overlay-rgb), 0.08);
  color: var(--text-primary);
}



.btn-form.save {
  background: var(--primary-gradient);
  color: #fff;
  box-shadow: var(--accent-glow);
}



.btn-form.save:hover {
  transform: translateY(-1px);
}

/* Curated Interactive Formula Viewer */
.formula-detail {
  padding: 16px;
  background: rgba(var(--overlay-rgb), 0.02);
  border-radius: 12px;
  border-left: 4px solid var(--color-high-school);
  margin-bottom: 16px;
}

.formula-latex {
  font-family: monospace;
  font-size: 1.2rem;
  color: var(--text-primary);
  margin-bottom: 8px;
  display: block;
}

.formula-explanation {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.4;
}

/* ==========================================================================
   ANIMATIONS & KEYFRAMES
   ========================================================================== */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes pulse {
  0% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.05); opacity: 0.8; }
  100% { transform: scale(1); opacity: 1; }
}

@keyframes messageSlide {
  from { opacity: 0; transform: translateY(12px) scale(0.95); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}

@keyframes modalScale {
  from { opacity: 0; transform: scale(0.95); }
  to { opacity: 1; transform: scale(1); }
}

/* Mobile Responsiveness */
@media (max-width: 992px) {
  .sidebar {
    width: 80px;
    padding: 24px 8px;
  }
  .logo-text, .nav-link span {
    display: none;
  }
  .logo-container {
    justify-content: center;
    padding-bottom: 16px;
  }
  .main-content {
    margin-left: 80px;
  }
  .grid-2col {
    grid-template-columns: 1fr;
  }
  .db-hero {
    grid-column: span 1;
  }
  .planner-layout, .focus-container {
    grid-template-columns: 1fr;
  }
  .top-header {
    padding: 0 20px;
  }
}
